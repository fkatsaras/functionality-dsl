{# props.colNames: list of column keys, e.g. ["id","name","year"] #}
{# props.endpointPath: the bound InternalREST endpoint #}

<Table
  name="{{ common.name }}"
  url="{{ props.get('endpointPath') }}"
  colNames={{ "{" }}{{ props.get('colNames') | tojson }}{{ "}" }}
/>