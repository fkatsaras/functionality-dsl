# Frontend Environment Configuration
# Auto-generated by FDSL

# Backend API URL (for development)
VITE_API_BASE_URL=http://{{ server.host }}:{{ server.port }}
{% if jwt_secret_value is defined and jwt_secret_value %}

# JWT Secret for client-side token generation (demo purposes only)
# In production, tokens should be generated server-side
VITE_JWT_SECRET={{ jwt_secret_value }}
{% endif %}
