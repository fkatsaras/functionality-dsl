import utils
import entity
import component
import server

FDSLModel:
    (imports+=Import)*
    (
        (servers+=Server)*
        (internalrest+=InternalRESTEndpoint)*
        (internalws+=InternalWSEndpoint)*
        (externalrest+=ExternalRESTEndpoint)*
        (externalws+=ExternalWSEndpoint)*
        (entities+=Entity)*
        (components+=Component)*
    )#
;
