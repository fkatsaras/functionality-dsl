import utils
import endpoints
import entity
import component
import server

FDSLModel:
    (imports+=Import)*
    (
        (servers+=Server)*
        (sources+=Source)*        // RESTEndpoint / WSEndpoint
        (entities+=Entity)*
        (components+=Component)*
    )#
;
