import utils
import integration
import endpoints
import entity
import component
import server

FDSLModel:
    (imports+=Import)*
    (
        (servers+=Server)*
        (external+=ExternalSource)*
        (entities+=Entity)*
        (internal+=InternalEndpoint)*
        (components+=Component)*
    )#
;
