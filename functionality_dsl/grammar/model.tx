import utils
import types
import datasource
import entity
import endpoint

Model:
    (imports+=Import)*
    (
        datasources      += Datasource
      | backendEntities  += BackendEntity
      | frontendEntities += FrontendEntity
      | endpoints        += Endpoint
    )*
;

Import: 'import' imported+=STRING ;