import utils
import entity
import component
import server

FDSLModel:
    (imports+=Import)*

    (
        servers+=Server
    |   apirest+=EndpointREST
    |   apiws+=EndpointWS
    |   externalrest+=SourceREST
    |   externalws+=SourceWS
    |   entities+=Entity
    |   components+=Component
    )+
;