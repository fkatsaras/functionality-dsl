import utils
import entity
import component
import server
import rbac
import auth

FDSLModel:
    (imports+=Import)*

    (
        roles+=Role
    |   authdb+=AuthDB
    |   auth+=Auth
    |   servers+=Server
    |   externalrest+=SourceREST
    |   externalws+=SourceWS
    |   entities+=Entity
    |   components+=Component
    )+
;