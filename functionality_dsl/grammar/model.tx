import entity
import utils
import datasource
import condition
import computed
import pipeline
import endpoint
import types

Model:
    (imports+=Import)? // optional single import block
    (
        (datasources+=Datasource)
        (entities+=Entity)
        (stepDefs+=StepDefinition)
        (pipelines+=Pipeline)
        (endpoints+=Endpoint)
    )#
;