import server
import database
import entity
import endpoints
import queries
import utils

Model:
    (imports+=Import)?
    (
        (server+=Server)*
        (databases+=Database)*
        (entities+=Entity)*
        (endpoints+=Endpoint)*
        (queries+=SQLQuery)*
    )#
;
