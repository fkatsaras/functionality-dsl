import utils
import entity
import component
import server

FDSLModel:
    (imports+=Import)*

    (
        servers+=Server
    |   apirest+=APIEndpointREST
    |   apiws+=APIEndpointWS
    |   externalrest+=SourceREST
    |   externalws+=SourceWS
    |   entities+=Entity
    |   components+=Component
    )+
;