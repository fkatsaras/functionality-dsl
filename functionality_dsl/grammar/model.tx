import server
import database
import entity
import endpoints
import queries
import websockets
import methods
import security
import utils

Model:
    (imports+=Import)?
    (
        (server+=Server)*
        (databases+=Database)*
        (entities+=Entity)*
        (endpoints+=Endpoint)*
        (queries+=SQLQuery)*
        (subscriptions+=Subscription)*
        (methods+=Method)* 
        (security+=Security)*
    )#
;
