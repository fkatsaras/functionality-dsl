import types
import utils

Datasource:
    'datasource' name=ID
        'kind:' KindEnum
        'uri:'  STRING
        ('options:' '-' options+=KeyValuePair['-'] )*
    )# 'end'
;

KeyValuePair:
    key=ID '=' value=STRING
;